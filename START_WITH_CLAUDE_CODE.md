# First prompt to Claude Code

Read `CLAUDE.md` first and treat it as the project contract.

Then:
1. run `npm install`
2. run `npm run build`
3. fix any errors
4. implement drag/drop, resize, lane snapping and edge editing
5. add undo/redo and autosave
6. add source-page/source-excerpt editing
7. improve editable PowerPoint export so each department can be exported individually or all together
8. keep RTL Arabic correct
9. do not invent workflow content
10. after each phase summarize changed files and remaining limitations